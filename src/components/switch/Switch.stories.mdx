import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, text } from '@storybook/addon-knobs';

import Switch from '.';

<Meta component={Switch} title="Components/Atoms/Switch" />

# Switch

<Description of={Switch} />
<ArgsTable of={Switch} />

<Story name="Switch with Knobs" parameters={{ docs: { disable: true } }}>
  <Switch
    checked={boolean('checked', false)}
    className={text('className', '')}
    disabled={boolean('disabled', false)}
    errorMsg={text('errorMsg', '')}
    handleChange={action('`Switch with Knobs` onChange event triggered.')}
    label={text('label', 'Accept Terms and Conditions')}
    name={text('name', 'termsAndConditions')}
    value={text('value', 'accepted')}
  />
</Story>

## Colors and States

### Default

<Canvas>
  <Story name="Default">
    <Switch label="I am a standard Switch" name="default" value="on" />
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story name="Disabled">
    <Switch
      label="This Switch is disabled"
      name="disabledChecked"
      value="disabledOn"
      disabled
    />
    <Switch
      label="So is this one"
      name="disabledUnchecked"
      value="disabledOff"
      checked
      disabled
    />
  </Story>
</Canvas>

### Error

<Canvas>
  <Story name="Error">
    <Switch
      label="This is a Switch with an error"
      name="error"
      value="on"
      errorMsg="This is an error message."
    />
  </Story>
</Canvas>
