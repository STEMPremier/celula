import { Description,
  Meta,
  Preview,
  Props,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, select, text } from '@storybook/addon-knobs';

import { Radio, RadioGroup } from '.';
import { COMPONENT_STATES as STATES } from '../../../utils/constants';

<Meta component={RadioGroup} title="Components/Molecules/Form Controls/RadioGroup" />

# RadioGroup and Radio
## RadioGroup
<Description of={RadioGroup} />
<Props of={RadioGroup} />

## Radio
<Description of={Radio} />
<Props of={Radio} />

<Story name="RadioGroups with Knobs" parameters={{ docs: {disable: true}}}>
  <form id="formKnobs">
    <RadioGroup
      className={text('className', '')}
      disabled={boolean('disabled', false)}
      errorClass={text('errorClass', '')}
      errorMsg={text('errorMsg', '')}
      formId={text('formId', 'formKnobs')}
      handleChange={action('`RadioGroups with Knobs` onChange event triggered.')}
      label={text('label', 'Choose your favorite OS:')}
      name={text('name', 'favoriteOS')}
      selectedValue={text('selectedValue', 'macos')}
    >
      <Radio label='macOS' value='macos' />
      <Radio label='Linux' value='linux' />
      <Radio label='Windows' value='windows' />
    </RadioGroup>
  </form>
</Story>

<Story name="Radios with Knobs" parameters={{ docs: {disable: true}}}>
  <Radio
    className={text('className', '')}
    label={text('label', 'macOS')}
    value={text('value', 'macos')}
  />
</Story>

### Types
<Preview>
  <Story name="RadioGroup Types">
    <form id="formTypes">
      <RadioGroup
        formId="formTypes"
        handleChange={action('`RadioGroups Types` onChange event triggered.')}
        handleChange={val => console.log(`The value is ${val}`)}
        label="Please make a choice:"
        name="choice"
        selectedValue="no"
      >
        <Radio label="Yes" value="yes" />
        <Radio label="No" value="no" />
      </RadioGroup>
    </form>
  </Story>
</Preview>


### States
<Preview>
  <Story name="RadioGroup States">
    <form id="formStates">
      {STATES.map(state => (
        <RadioGroup
          disabled={state === 'disabled'}
          errorMsg={state === 'error' ? 'You did not choose wisely!' : ''}
          formId="formStates"
          handleChange={action('`RadioGroups States` onChange event triggered.')}
          label={`${state.charAt(0).toUpperCase() + state.slice(1)} RadioGroup:`}
          name={`${state}Group`}
          selectedValue="thatOne"
        >
          <Radio label="This one" value="thisOne" />
          <Radio label="That one" value="thatOne" />
        </RadioGroup>
      ))}
    </form>
  </Story>
</Preview>
