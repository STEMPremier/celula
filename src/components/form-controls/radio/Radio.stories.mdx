import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import { Radio, RadioGroup } from '.';

<Meta
  args={{
    children: [
      <Radio label='macOS' value='macos' />,
      <Radio label='Linux' value='linux' />,
      <Radio label='Windows' value='windows' />,
    ],
    handleChange: action('RadioGroup Changed!'),
    label: 'Choose your favorite OS:',
    name: 'withControls',
    selectedValue: 'macos',
  }}
  component={RadioGroup}
  title="Components/Molecules/Form Controls/Radio"
  subcomponents={{Radio}}
/>

# Radio
<Description of={Radio} />
<Description of={RadioGroup} />

<Canvas>
  <Story name="Radios with Controls">
    {args => <RadioGroup {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Radios with Controls" />

## States
A `RadioGroup` can be disabled, or show an error.

<Canvas isColumn>
  <Story
    args={{
      name: 'defaultRadioGroup',
    }}
    name="Default Radios"
  >
    {args => <RadioGroup {...args} />}
  </Story>
  <Story
    args={{
      disabled: true,
      name: 'disabledRadioGroup',
    }}
    name="Disabled Radios"
  >
    {args => <RadioGroup {...args} />}
  </Story>
  <Story
    args={{
      errorMsg: 'You may not select Windows!',
      name: 'erroeRadioGroup',
      selectedValue: 'windows',
    }}
    name="Error Radios"
  >
    {args => <RadioGroup {...args} />}
  </Story>
</Canvas>
