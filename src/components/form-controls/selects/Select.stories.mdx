import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, text, array } from '@storybook/addon-knobs';

import Select from './Select';

<Meta component={Select} title="Components/Molecules/Form Controls/Select" />

# Select

<Description of={Select} />
<Props of={Select} />

<Story name="Select" parameters={{ docs: {disable: true}}}>
  <form>
  <Select
    className={text('className', '')}
    handleChange={action('click')}
    label={text('label', 'Default Fieldset Label')}
    name={text('name', 'Default Name for Select')}
    form={text('form', '')}
    options={array('options', [
      {
        name: 'option 1',
        value: 1,
      },
      {
        name: 'option 2',
        value: 2,
      },
    ],)} />
    </form>
</Story>

# Enabled Select Using a Label
<Preview>
  <Story name="Enabled Select Using Label">
    <form>
      <Select 
        label = "This guidance text goes in the label"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
        handleChange={action('changed')}
        form="formName1"
      />
    </form> 
  </Story>
</Preview>

# Enabled Select Using a Default Value
<Preview>
  <Story name="Enabled Select Using a Default Value">
    <form>
      <Select 
        value="option 3"
        label="Label is always required for accessbility but this one will not be show to the user"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
        handleChange={action('changed')}
        form="formName2"
      />
    </form> 
  </Story>
</Preview>

# Disabled Select Using Label  confirm if disabled and error are actually going to exist on this component 
<Preview>
  <Story name="Disabled Select ">
    <form>
      <Select 
        label = "Select from the below choices"
        handleChange={action('changed')}
        form="formName3"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form> 
  </Story>
</Preview>





