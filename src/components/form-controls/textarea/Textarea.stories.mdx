import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';

import Textarea from '.';

<Meta
  args={{
    label: 'Textarea',
    name: 'demoTextarea',
  }}
  component={Textarea}
  title="Components/Molecules/Form Controls/Textarea"
/>

# Textarea
<Description of={Textarea} />

<Canvas>
  <Story name="Textarea with Controls">
    {args => <Textarea {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Textarea with Controls" />

## Options
A `Textarea` can have help text.

<Canvas>
  <Story name="Textarea with Help Text">
    <Textarea
      helpText="I am helpful text."
      label="Textarea with help text:"
      name="withHelpText"
      placeholder="Please fill this in."
    />
  </Story>
</Canvas>

## States
A `Textarea` can be disabled, or have an error.

<Canvas isColumn>
  <Story name="Disabled Textarea">
    <Textarea
      disabled
      label="Fill me in, I dare you:"
      name="disabledTextarea"
      placeholder="I double-dog dare you."
    />
  </Story>
  <Story name="Error Textarea">
    <Textarea
      errorMsg="You were wrong!"
      label="Try NOT to be wrong:"
      name="errorTextarea"
      placeholder="Please fill this in."
    />
  </Story>
</Canvas>
