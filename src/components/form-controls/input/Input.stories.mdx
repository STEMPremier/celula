import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, select, text } from '@storybook/addon-knobs';

import Input from '.';
import { INPUT_TYPES as TYPES } from '../../../utils/constants';

<Meta component={Input} title="Components/Molecules/Form Controls/Input" />

# Inputs
<Description of={Input} />
<Props of={Input} />

<Story name="Input wiht Knobs" parameters={{ docs: { disable: true } }}>
  <form id="inputWithKnobForm">
    <Input
      btnOptions={{
        btnClick: action('`Input with Knobs` button onClick event triggered.'),
        btnIcon: text('btnIcon', 'navigate', 'Button Options'),
      }}
      className={text('className', '', 'Input Options')}
      disabled={boolean('disabled', false, 'Input Options')}
      errorMsg={text('errorMsg', '', 'Input Options')}
      formId={text('formId', 'inputWithKnobsForm', 'Input Options')}
      handleChange={action('`Input with Knobs` onChange event triggered.')}
      helpText={text('helpText', 'This is useful or helpful information.', 'Input Options')}
      htmlType={text('htmlType', 'text', 'Input Options')}
      icon={text('icon', '', 'Input Options')}
      initialValue={text('initialValue', '','Input Options')}
      label={text('label', 'Input with Knobs', 'Input Options')}
      name={text('name', 'inputWithKnobs', 'Input Options')}
      placeholder={text('placeholder', 'Please fill this in.', 'Input Options')}
    />
  </form>
</Story>

### Types
<Preview>
  <Story name="Input Types">
    <form id="inputTypesForm">
      {TYPES.map(type => (
        <div key={type}>
          <Input
            formId="inputTypesForm"
            htmlType={type}
            label={`Label for ${type}`}
            name={`${type}InputStandard`}
          />
          <Input
            formId="inputTypesForm"
            helpText={`Help text for ${type}`}
            htmlType={type}
            label={`Label for ${type}`}
            name={`${type}InputHelpText`}
          />
          <Input
            formId="inputTypesForm"
            htmlType={type}
            icon="edit"
            label={`Label for ${type}`}
            name={`${type}InputIcon`}
          />
          <Input
            btnOptions={{
              btnClick: action(`'Input Types' of type ${type} button onClick event triggered.`),
              btnIcon: 'navigate',
            }}
            formId="inputTypesForm"
            htmlType={type}
            label={`Label for ${type}`}
            name={`${type}InputButton`}
          />
        </div>
      ))}
    </form>
  </Story>
</Preview>

### Input States
<Preview>
  <Story name="Input States">
    <form id="inputStatesForm">
      <h2>Default</h2>
      <Input
        formId="inputStatesForm"
        label="Label for Standard"
        name="defaultTextInput"
      />
      <Input
        formId="inputStatesForm"
        helpText="Help text for default"
        label="Label for Disabled"
        name="defaultTextInputWithHelpText"
      />
      <Input
        formId="inputStatesForm"
        icon="edit"
        label="Label for Icon"
        name="defaultTextInputWithIcon"
      />
      <Input
        btnOptions={{
          btnClick: action(`'Input Types' button onClick event triggered.`),
          btnIcon: 'navigate',
        }}
        formId="inputStatesForm"
        label="Label for Button"
        name="defaultTextInputWithButton"
      />
      <h2>Disabled</h2>
      <Input
        disabled
        formId="inputStatesForm"
        label="Label for Standard"
        name="disabledTextInput"
      />
      <Input
        disabled
        formId="inputStatesForm"
        helpText="Help text for disabled"
        label="Label for Disabled"
        name="disabledTextInputWithHelpText"
      />
      <Input
        disabled
        formId="inputStatesForm"
        icon="edit"
        label="Label for Icon"
        name="disabledTextInputWithIcon"
      />
      <Input
        disabled
        btnOptions={{
          btnClick: action(`'Input Types' disabled button onClick event triggered.`),
          btnIcon: 'navigate',
        }}
        formId="inputStatesForm"
        label="Label for Button"
        name="disabledTextInputWithButton"
      />
      <h2>Error</h2>
      <Input
        errorMsg="This is an error message."
        formId="inputStatesForm"
        label="Label for Standard"
        name="errorTextInput"
      />
      <Input
        errorMsg="This is an error message."
        formId="inputStatesForm"
        helpText="Help text for error"
        label="Label for Disabled"
        name="errorTextInputWithHelpText"
      />
      <Input
        errorMsg="This is an error message."
        formId="inputStatesForm"
        icon="edit"
        label="Label for Icon"
        name="errorTextInputWithIcon"
      />
      <Input
        btnOptions={{
          btnClick: action(`'Input Types' error button onClick event triggered.`),
          btnIcon: 'navigate',
        }}
        errorMsg="This is an error message."
        formId="inputStatesForm"
        label="Label for Button"
        name="errorTextInputWithButton"
      />
    </form>
  </Story>
</Preview>
