import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import FormControlGroup from '.';
import { Checkbox } from '../../checkbox';
import { Radio } from '../../radio';

<Meta
  args={{
    children: [
      <Checkbox label='macOS' value='macos' />,
      <Checkbox label='Linux' value='linux' />,
      <Checkbox label='Windows' value='windows' />,
    ],
    handleChange: action('FormControlGroup Changed!'),
    label: 'Choose your favorite OS:',
    name: 'withControls',
    selectedValue: 'macos',
  }}
  component={FormControlGroup}
  title="Components/Molecules/Form Controls/Core/FormControlGroup"
/>

# FormControlGroup
<Description of={FormControlGroup} />

<Canvas>
  <Story name="FormControlGroup with Controls">
    {args => <FormControlGroup {...args} />}
  </Story>
</Canvas>

<ArgsTable story="FormControlGroup with Controls" />

## Grouping Checkboxes
<Canvas>
  <Story name="FormControlGroup with Checkboxes">
    <FormControlGroup
      label="Test Group"
      name="myGroup"
      handleChange={action('`FormControlGroup with Checkboxes` onChange event triggered.')}
      selectedValues={['checkboxOne']}
    >
      <Checkbox label="Checkbox 1" value="checkboxOne" />
      <Checkbox label="Checkbox 2" value="checkboxTwo" />
    </FormControlGroup>
  </Story>
</Canvas>

## Grouping Radios
<Canvas>
  <Story name="FormControlGroup with Radios">
    <FormControlGroup
      label="Test Group"
      name="myGroup"
      handleChange={action('`FormControlGroup with Checkboxes` onChange event triggered.')}
      selectedValues={['radioOne']}
    >
      <Radio label="Radio 1" value="radioOne" />
      <Radio label="Radio 2" value="radioTwo" />
    </FormControlGroup>
  </Story>
</Canvas>
