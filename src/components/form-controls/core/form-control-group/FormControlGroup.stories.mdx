import { Description, Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import FormControlGroup from '.';
import { Checkbox } from '../../checkbox';
import { Radio } from '../../radio';

<Meta component={FormControlGroup} title="Components/Molecules/Form Controls/Core/FormControlGroup" />

# FormControlGroup
<Description of={FormControlGroup} />
<Props of={FormControlGroup} />

## Grouping Checkboxes
<Preview>
  <Story name="FormControlGroup with Checkboxes">
    <FormControlGroup
      label="Test Group"
      name="myGroup"
      handleChange={action('`FormControlGroup with Checkboxes` onChange event triggered.')}
      selectedValues={['checkboxOne']}
    >
      <Checkbox label="Checkbox 1" value="checkboxOne" />
      <Checkbox label="Checkbox 2" value="checkboxTwo" />
    </FormControlGroup>
  </Story>
</Preview>

## Grouping Radios
<Preview>
  <Story name="Form Control Group with Radios">
    <FormControlGroup
      label="Test Group"
      name="myGroup"
      handleChange={action('`FormControlGroup with Checkboxes` onChange event triggered.')}
      selectedValues={['radioOne']}
    >
      <Radio label="Radio 1" value="radioOne" />
      <Radio label="Radio 2" value="radioTwo" />
    </FormControlGroup>
  </Story>
</Preview>
