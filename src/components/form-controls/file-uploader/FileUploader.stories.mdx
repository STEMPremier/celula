import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, select, text } from '@storybook/addon-knobs';

import {
  COLORS,
  FILEUPLOADER_TYPES as TYPES,
  SIZES,
} from '../../../utils/constants';

import FileUploader from '.';

<Meta component={FileUploader} title="Components/Molecules/Form Controls/FileUploader" />

# FileUplaoder
<Description of={FileUploader} />
<ArgsTable of={FileUploader} />

<Story name="FileUplaoder with Knobs" parameters={{ docs: { disable: true } }}>
  <form id="fileUploaderWithKnobForm">
    <FileUploader
      accept={text('accept', '')}
      capture={select('capture', ['', 'user', 'environment'])}
      className={text('className', '')}
      color={select('color', COLORS, 'primary')}
      disabled={boolean('disabled', false)}
      errorMsg={text('errorMsg', '')}
      formId={text('formId', 'fileUploaderWithKnobForm')}
      handleClick={action('`FileUploader with Knobs` onChange event triggered.')}
      multiple={boolean('multiple', false)}
      name={text('name', 'fileUploaderWithKnobs')}
      size={select('size', SIZES, 'small')}
      type={select('type', TYPES, 'default')}
    >
      {text('children', 'CHILDREN LABEL')}
    </FileUploader>
  </form>
</Story>

## Solid FileUploader (Default)
<Canvas>
  <Story name="Solid Small">
     <FileUploader
        accept="*"
        name="solid_small"
        size="small"
      >
        small uploader
      </FileUploader>
  </Story>
  <Story name="Solid Large (Default)">
     <FileUploader
        accept="*"
        name="solid_large"
        size="large"
      >
        large uploader
      </FileUploader>
  </Story>
  <Story name="Solid Jumbo">
     <FileUploader
        accept="*"
        name="solid_jumbo"
        size="jumbo"
      >
        jumbo uploader
      </FileUploader>
  </Story>
</Canvas>

### Colors and States
<Canvas>
  <Story name="Solid Primary (Default)">
    <FileUploader
      accept="*"
      color="primary"
      name="soild_primary"
      size="small"
    >
      primary
    </FileUploader>
  </Story>
  <Story name="Solid Secondary">
    <FileUploader
      accept="*"
      color="secondary"
      name="soild_secondary"
      size="small"
    >
      secondary
    </FileUploader>
  </Story>
  <Story name="Solid Black">
    <FileUploader
      accept="*"
      color="black"
      name="soild_black"
      size="small"
    >
      black
    </FileUploader>
  </Story>
  <Story name="Solid Inverted">
    <div style={{backgroundColor:'black'}}>
      <FileUploader
        accept="*"
        color="inverted"
        name="soild_inverted"
        size="small"
      >
        inverted
      </FileUploader>
    </div>
  </Story>
  <Story name="Solid Disabled">
    <FileUploader
      accept="*"
      disabled={true}
      name="soild_disabled"
      size="small"
    >
      disabled
    </FileUploader>
  </Story>
</Canvas>

## Outline FileUploaders
<Canvas>
  <Story name="Outline Small">
     <FileUploader
        accept="*"
        name="outline_small"
        size="small"
        type="outline"
      >
        small uploader
      </FileUploader>
  </Story>
  <Story name="Outline Large (Default)">
     <FileUploader
        accept="*"
        name="outline_large"
        size="large"
        type="outline"
      >
        large uploader
      </FileUploader>
  </Story>
  <Story name="Outline Jumbo">
     <FileUploader
        accept="*"
        name="outline_jumbo"
        size="jumbo"
        type="outline"
      >
        jumbo uploader
      </FileUploader>
  </Story>
</Canvas>

### Colors and States
<Canvas>
  <Story name="Outline Primary (Default)">
    <FileUploader
      accept="*"
      color="primary"
      name="outline_primary"
      size="small"
      type="outline"
    >
      primary
    </FileUploader>
  </Story>
  <Story name="Outline Secondary">
    <FileUploader
      accept="*"
      color="secondary"
      name="outline_secondary"
      size="small"
      type="outline"
    >
      secondary
    </FileUploader>
  </Story>
  <Story name="Outline Black">
    <FileUploader
      accept="*"
      color="black"
      name="outline_black"
      size="small"
      type="outline"
    >
      black
    </FileUploader>
  </Story>
  <Story name="Outline Inverted">
    <div style={{backgroundColor:'black'}}>
      <FileUploader
        accept="*"
        color="inverted"
        name="outline_inverted"
        size="small"
        type="outline"
      >
        inverted
      </FileUploader>
    </div>
  </Story>
  <Story name="Outline Disabled">
    <FileUploader
      accept="*"
      disabled={true}
      name="outline_disabled"
      size="small"
      type="outline"
    >
      disabled
    </FileUploader>
  </Story>
</Canvas>
