import { Description, Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, select, text, array } from '@storybook/addon-knobs';

import { Checkbox } from '.';
import { COMPONENT_STATES as STATES } from '../../../utils/constants';

<Meta component={Checkbox} title="Components/Molecules/Form Controls/Checkbox"/>

# Checkbox
<Description of={Checkbox} />
<Props of={Checkbox}/>

<Story name="Checkbox with Knobs" parameters={{docs: {disable: true}}}>
  <form id="formKnobs">
    <Checkbox
      checked={boolean('checked', false )}
      className={text('className', '')}
      disabled={boolean('disabled', false)}
      errorMsg={text('errorMsg', '')}
      formId={text('formId', 'formKnobs')}
      handleClick={action('`Checkbox with Knobs` onClick event triggered.')}
      label={text('label', 'Accept Terms and Conditions')}
      name={text('name', 'termsAndConditions')}
      value={text('value', 'accepted')}
    />
  </form>
</Story>

### Types
<Preview>
  <Story name="Checkbox Types">
    <form id="formTypes">
      <Checkbox
        checked
        formId="formTypes"
        label="I am checked"
        name="checked"
        value={true}
      />
      <Checkbox
        formId="formTwo"
        label="I am unchecked"
        name="unchecked"
        value={false}
      />
    </form>
  </Story>
</Preview>


### States
<Preview>
  <Story name="Checkbox States">
    <form id="formStates">
      {STATES.map(state => (
        <Checkbox
          disabled={state === 'disabled'}
          errorMsg={state === 'error' && "There is an error."}
          formId="formStates"
          label={`${state.charAt(0).toUpperCase()}${state.slice(1)} Checkbox`}
          name={`${state}_checkbox`}
          value={state}
        />
      ))}
    </form>
  </Story>
</Preview>
