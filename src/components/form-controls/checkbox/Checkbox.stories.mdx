import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import { Checkbox } from '.';

<Meta
  args={{
    label: 'Troy and Abed In The Morning',
    handleChange: action('Checkbox checked!'),
    name: 'troyAbed',
    value: 'troyAbed',
  }}
  component={Checkbox}
  title="Components/Molecules/Form Controls/Checkbox"
/>

# Checkbox
<Description of={Checkbox} />

<Canvas>
  <Story name="Checkbox with Controls">
    {args => <Checkbox {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Checkbox with Controls" />

## Colors and States
A `Checkbox` can be disabled, or show an error.

<Canvas isColumn>
  <Story name="Default Checkbox">
    <Checkbox
      label="I am standard checkbox"
      name="default"
      value={true}
    />
  </Story>
  <Story name="Disabled Checkbox">
    <Checkbox
      disabled
      label="I am a disabled checkbox"
      name="disabled"
      value={true}
    />
  </Story>
  <Story name="Error Checkbox">
    <Checkbox
      errorMsg="There is an error message."
      label="This is a checkbox with an error"
      name="error"
      value={true}
    />
  </Story>
</Canvas>
