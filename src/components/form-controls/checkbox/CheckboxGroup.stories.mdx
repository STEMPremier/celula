import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import { Checkbox, CheckboxGroup } from '.';

<Meta
  args={{
    children: [
      <Checkbox label="Vim" value="vim" />,
      <Checkbox label="Emacs" value="emacs" />,
      <Checkbox label="VS Code" value="vscode" />,
      <Checkbox label="Atom" value="atom" />,
    ],
    handleChange: action('CheckboxGroup changed!'),
    label: 'What is your favorite Text Editor?',
    name: 'withControls'
  }}
  component={CheckboxGroup}
  title="Components/Molecules/Form Controls/CheckboxGroup"
  subcomponents={{Checkbox}}
/>

# CheckboxGroup
<Description of={CheckboxGroup} />

<Canvas>
  <Story name="CheckboxGroup with Controls">
    {args => <CheckboxGroup {...args} />}
  </Story>
</Canvas>

<ArgsTable story="CheckboxGroup with Controls" />

## States
A `CheckboxGroup` can be disabled, allow an individual `Checkbox` to be disabled, or show an error.

<Canvas isColumn>
  <Story
    args={{
      name: 'default',
      selectedValues: ['vim'],
    }}
    name="Default CheckboxGroup"
  >
    {args => <CheckboxGroup {...args} />}
  </Story>
  <Story
    args={{
      disabled: true,
      name: 'disabled',
      selectedValues: ['vim'],
    }}
    name="Disabled CheckboxGroup (Group)"
  >
    {args => <CheckboxGroup {...args} />}
  </Story>
  <Story
    args={{
      children: [
        <Checkbox label="Vim" value="vim" />,
        <Checkbox disabled label="Emacs" value="emacs" />,
        <Checkbox label="VS Code" value="vscode" />,
        <Checkbox label="Atom" value="atom" />,
      ],
      name: 'disabled2',
      selectedValues: ['vim'],
    }}
    name="Disabled CheckboxGroup (Individual)"
  >
    {args => <CheckboxGroup {...args} />}
  </Story>
  <Story
    args={{
      errorMsg: 'You cannot select anything except Vim as your favorite.',
      name: 'error',
      selectedValues: ['vim', 'emacs'],
    }}
    name="Error CheckboxGroup"
  >
    {args => <CheckboxGroup {...args} />}
  </Story>
</Canvas>
