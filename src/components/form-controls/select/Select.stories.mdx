import { Description, Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, number, radios, text } from '@storybook/addon-knobs';

import Select from '.';

<Meta component={Select} title="Components/Molecules/Form Controls/Select" />

# Select
<Description of={Select} />
<Props of={Select} />

<Story name="Select with Knobs" parameters={{ docs: { disable: true } }}>
  <form id="selectWithKnobsForm">
    <Select
      btnOptions={{
        btnClick: action('`Select with Knobs` button onClick event triggered.'),
        btnIcon: text('btnIcon', 'navigate', 'Button Options'),
      }}
      className={text('className', '', 'Select Options')}
      disabled={boolean('disabled', false, 'Select Options')}
      errorMsg={text('errorMsg', '', 'Select Options')}
      formId={text('formId', 'selectWithKnobForm', 'Select Options')}
      handleChange={action('`Select with Knobs` onChange event triggered.')}
      helpText={text('helpText', 'This is useful or helpful information.', 'Select Options')}
      icon={text('icon', '', 'Select Options')}
      label={text('label', 'Select with Knobs', 'Select Options')}
      name={text('name', 'selectWithKnobs', 'Select Options')}
      options={[
        {
          name: 'Option 1',
          value: '1',
        },
        {
          name: 'Option 2',
          value: '2',
        }
      ]}
      placeholder={text('placeholder', 'Please make a choice', 'Select Options')}
      selectedValue={radios('selectedValue', { None: 'none', 'Option 1': '1', 'Option 2': '2' }, 'none', 'Select Options')}
    />
  </form>
</Story>

### Types
<Preview>
  <Story name="Select Types">
    <form>
      <Select
        handleChange={action('`Select Types` Default Select onChange event triggered.')}
        label="Default Select"
        name="defaultSelect"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        handleChange={action('`Select Types` Default Select with Help Text onChange event triggered.')}
        helpText="This is useful or helpful information."
        label="Default Select with Help Text"
        name="defaultSelectWithHelpText"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        handleChange={action('`Select Types` with Icon onChange event triggered.')}
        icon="edit"
        label="Default Select with Icon"
        name="defaultSelectWithIcon"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        btnOptions={{
          btnClick: action('`Select Types` Default Select with Button button onClick event triggered'),
          btnIcon: "add",
        }}
        handleChange={action('`Select Types` Default Select with Button onChange event triggered.')}
        label="Default Select with Button"
        name="defaultSelectWithButton"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form>
  </Story>
</Preview>

### States
<Preview>
  <Story name="Select States">
    <form>
      <h2>Default</h2>
      <Select
        handleChange={action('`Select States` Default Select onChange event triggered.')}
        label="Default Select"
        name="defaultSelect2"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        handleChange={action('`Select States` Default Select with Help Text onChange event triggered.')}
        helpText="This is useful or helpful information."
        label="Default Select with Help Text"
        name="defaultSelectwithHelpText2"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        handleChange={action('`Select States` Default Select with Icon onChange event triggered.')}
        icon="calendar"
        label="Default Select with Icon"
        name="defaultSelectWithIcon2"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        btnOptions={{
          btnClick: action('`Select States` Default Select with Button button onClick event triggered'),
          btnIcon: "add",
        }}
        handleChange={action('`Select States` Default Select with Button onChange event triggered.')}
        label="Default Select with button"
        name="defaultSelectWithButton2"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <h2>Disabled</h2>
      <Select
        disabled
        handleChange={action('`Select States` Disabled Select onChange event triggered.')}
        label="Disabled Select"
        name="disabledSelect"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        disabled
        handleChange={action('`Select States` Disabled Select with Help Text onChange event triggered.')}
        helpText="This is useful or helpful information."
        label="Disabled Select with Help Text"
        name="disabledSelectWithHelpText"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        disabled
        handleChange={action('`Select States` Disabled Select with Icon onChange event triggered.')}
        icon="calendar"
        label="Disabled Select with Icon"
        name="disabledSelectWithIcon"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        btnOptions={{
          btnClick: action('`Select States` Disabled Select with Button button onClick event triggered'),
          btnIcon: "add",
        }}
        disabled
        handleChange={action('`Select States` Disabled Select with Button onChange event triggered.')}
        label="Disabled Select with Button"
        name="disabledSelectWithButton"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <h2>Error</h2>
      <Select
        errorMsg="Error with Selection"
        handleChange={action('`Select States` Error Select onChange event triggered.')}
        label="Error Select"
        name="errorSelect"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        errorMsg="Error with Selection"
        handleChange={action('`Select States` Error Select with Help Text onChange event triggered.')}
        helpText="This is usefule or helpful information."
        label="Error Select with Help Text"
        name="errorSelectWithHelpText"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        errorMsg="Error with Selection"
        handleChange={action('`Select States` Error Select with Icon onChange event triggered.')}
        icon="calendar"
        label="Error Select with Icon"
        name="errorSelectWithIcon"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        btnOptions={{
          btnClick: action('`Select States` Error Select with Button button onClick event triggered'),
          btnIcon: "add",
        }}
        errorMsg="Error with Selection"
        handleChange={action('`Select States` Error Select with Button onChange event triggered.')}
        label="Error Select with Button"
        name="errorSelect"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form>
  </Story>
</Preview>
