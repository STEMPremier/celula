import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import Select from '.';

<Meta
  args={{
    handleChange: action('Selected!'),
    label: 'Select',
    name: 'defaultSelect',
    options: [
      {
        name: 'Option 1',
        value: '1',
      },
      {
        name: 'Option 2',
        value: '2',
      }
    ],
  }}
  component={Select}
  title="Components/Molecules/Form Controls/Select"
/>

# Select
<Description of={Select} />

<Canvas>
  <Story name="Select with Controls">
    {args => <Select {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Select with Controls" />

## Options
A `Select` has several options avaliable. It can have help text, a helpfult icon, or an attached
button. Or it can have all of them at the same time.

You can put any of the `SystemIcons` on the attached button.

<Canvas isColumn>
  <Story
    args={{
      helpText: 'This is useful or helpful information.',
      label: 'Select with Help Text:',
      name: 'withHelpText',
    }}
    name="Select with Help Text"
  >
    {args => <Select {...args} />}
  </Story>
  <Story
    args={{
      icon: 'edit',
      label: 'Select with an Icon:',
      name: 'withIcon',
    }}
    name="Select with an Icon"
  >
    {args => <Select {...args} />}
  </Story>
  <Story
    args={{
      btnOptions: {
        btnClick: action('Select button clicked!'),
        btnIcon: 'navigate',
      },
      label: 'Select with Attached Button',
      name: 'withAttachedButton',
    }}
    name="Select with a Button"
  >
    {args => <Select {...args} />}
  </Story>
  <Story
    args={{
      btnOptions: {
        btnClick: action('Select button clicked!'),
        btnIcon: 'add',
      },
      helpText: 'Hold the pickles.',
      icon: 'edit',
      label: 'Select with everything:',
      name: 'selectWithEverything',
    }}
    name="Select with Everything"
  >
    {args => <Select {...args} />}
  </Story>
</Canvas>

## States
A `Select` can be disabled, or report an error.

<Canvas isColumn>
  <Story
    args={{
      disabled: true,
      label: 'Make a choice, I dare you:',
      name: 'disbaledSelect',
    }}
    name="Disabled Select"
  >
    {args => <Select {...args} />}
  </Story>
  <Story
    args={{
      errorMsg: 'You chose poorly.',
      label: 'Try NOT to be wrong:',
      name: 'errorSelect',
    }}
    name="Error Select"
  >
    {args => <Select {...args} />}
  </Story>
</Canvas>
