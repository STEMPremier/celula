import { Description, Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, number, radios, text } from '@storybook/addon-knobs';

import Select from '.';

<Meta component={Select} title="Components/Molecules/Form Controls/Select" />

# Select
<Description of={Select} />
<Props of={Select} />

<Story name="Select with Knobs" parameters={{docs:{disable: true}}}>
  <form>
    <Select
      buttonClick={action('`Select with Knobs` button onClick event triggered.')}
      buttonIcon={text('icon', 'navigate')}
      buttonShow={boolean('buttonShow', true)}
      className={text('className', '')}
      disabled={boolean('disabled', false)}
      errorMsg={text('errorMsg', '')}
      formId={text('formId', '')}
      handleChange={action('`Select with Knobs` onChange event triggered.')}
      label={text('label', 'Default Fieldset Label')}
      name={text('name', 'selectWithKnobs')}
      options={[
        {
          name: 'Option 1',
          value: 1,
        },
        {
          name: 'Option 2',
          value: 2,
        }
      ]}
      selectedValue={radios('selectedValue', ['None', 1, 2], 'none')}
    />
  </form>
</Story>

# Select Types
<Preview>
  <Story name="Select Types">
    <form>
      <Select
        handleChange={action('`Select Types` Default Select onChange event triggered.')}
        label="Default Select"
        name="defaultSelect"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        buttonClick={action('`Select Types` Default Select with Button button onClick event triggered')}
        buttonIcon="add"
        buttonShow
        handleChange={action('`Select Types` Default Select with Button onChange event triggered.')}
        label="Default Select with Button"
        name="defaultSelectWithButton"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form>
  </Story>
</Preview>

# Select States
<Preview>
  <Story name="Select States">
    <form>
      <Select
        handleChange={action('`Select States` Default Select onChange event triggered.')}
        label="Default Select"
        name="defaultSelect2"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        buttonClick={action('`Select States` Default Select with Button button onClick event triggered')}
        buttonIcon="add"
        buttonShow
        handleChange={action('`Select States` Default Select with Button onChange event triggered.')}
        label="Default Select with button"
        name="defaultSelectWithButton"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        disabled
        handleChange={action('`Select States` Disabled Select onChange event triggered.')}
        label="Disabled Select"
        name="disabledSelect"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        buttonClick={action('`Select States` Disabled Select with Button button onClick event triggered')}
        buttonIcon="add"
        buttonShow
        disabled
        handleChange={action('`Select States` Disabled Select with Button onChange event triggered.')}
        label="Disabled Select with Button"
        name="disabledSelectWithButton"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        errorMsg="Error with Selection"
        handleChange={action('`Select States` Error Select onChange event triggered.')}
        label="Error Select"
        name="errorSelect"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
      <Select
        buttonClick={action('`Select States` Error Select with Button button onClick event triggered')}
        buttonIcon="add"
        buttonShow
        errorMsg="Error with Selection"
        handleChange={action('`Select States` Error Select onChange event triggered.')}
        label="Error Select"
        name="errorSelect"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form>
  </Story>
</Preview>
