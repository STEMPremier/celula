import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, text, array, number } from '@storybook/addon-knobs';

import Select from '.';

<Meta component={Select} title="Components/Molecules/Form Controls/Select" />

# Select

<Description of={Select} />
<Props of={Select} />

<Story name="Select" parameters={{ docs: {disable: true}}}>
  <form>
  <Select
    className={text('className', '')}
    handleChange={action('clicked handleChange in the select ')}
    rightIconClick={action('clicked rightArrowClick in the gradient')}
    label={text('label', 'Default Fieldset Label')}
    selectedValue={text('selectedValue', '2')}
    showArrow
    name={text('name', 'Default Name for Select')}
    formId={text('form', '')}
    options={array('options', [
      {
        name: 'Option 1',
        value: 1,
      },
      {
        name: 'Option 2',
        value: 2,
      },
    ],)} />
    </form>
</Story>

# Enabled Select Using a Label
<Preview>
  <Story name="Enabled Select Using Label">
    <form>
      <Select 
        label = "This guidance text goes in the label"
        name="Enabled Select Using Label"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
        handleChange={action('changed')}
        rightIconClick={action('rightArrowClick')}
        formId="formName1"
        showArrow
      />
    </form> 
  </Story>
</Preview>

# Enabled Select Using a Default Value
<Preview>
  <Story name="Enabled Select Using a Default Value">
    <form>
      <Select 
        selectedValue={2}
        rightIconClick={action('rightArrowClick')}
        name="Enabled Select Using a Default Value"
        label="Label is always required for accessbility"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
        handleChange={action('changed')}
        formId="formName2"
        showIcon
      />
    </form> 
  </Story>
</Preview>

# Disabled Select Using Label  
<Preview>
  <Story name="Disabled Select ">
    <form>
      <Select 
        label = "Select from the below choices"
        handleChange={action('changed')}
        rightIconClick={action('rightArrowClick')}
        formId="formName3"
        name="Disabled Select "
        showIcon
        disabled
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form> 
  </Story>
</Preview>

# Select without right arrow 
<Preview>
  <Story name="Select without the right arrow">
    <form>
      <Select 
        label = "Select from the below choices"
        handleChange={action('changed')}
        formId="formName4"
        name="Select Simple "
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form> 
  </Story>
</Preview>

# Select Error
<Preview>
  <Story name="Select Error">
    <form>
      <Select 
        label="Select from the below choices"
        handleChange={action('changed')}
        formId="formName5"
        name="Select Error no arrow"
        errorMsg="Error Message"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form> 
  </Story>
</Preview>

# Select Error With Show Arrow
<Preview>
  <Story name="Select Error with right arrow">
    <form>
      <Select 
        label = "Select from the below choices"
        handleChange={action('changed')}
        formId="formName6"
        name="Select Error with right arrow"
        errorMsg="Error Message"
        showIcon
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form> 
  </Story>
</Preview>







