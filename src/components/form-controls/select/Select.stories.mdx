import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, text, array, number } from '@storybook/addon-knobs';

import Select from '.';

<Meta component={Select} title="Components/Molecules/Form Controls/Select" />

# Select

<Description of={Select} />
<Props of={Select} />

<Story name="Select" parameters={{ docs: {disable: true}}}>
  <form>
  <Select
    buttonClick={action('`Select with Knobs` button onClick event triggered.')}
    buttonIcon={text('icon', 'navigate')}
    buttonShow={boolean('buttonShow', true)}
    className={text('className', '')}
    disabled={boolean('disabled', false)}
    errorMsg={text('errorMsg', '')}
    formId={text('form', '')}
    handleChange={action('`Select with Knobs` onChange event triggered.')}
    label={text('label', 'Default Fieldset Label')}
    name={text('name', 'selectWithKnobs')}
    options={[
      {
        name: 'Option 1',
        value: 1,
      },
      {
        name: 'Option 2',
        value: 2,
      }
    ]}
    selectedValue={text('selectedValue', '2')}
  />
    </form>
</Story>

# Enabled Select Using a Label
<Preview>
  <Story name="Enabled Select Using Label">
    <form>
      <Select 
        label = "This guidance text goes in the label"
        name="enabledSelectUsingLabel"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
        handleChange={action('changed')}
        buttonClick={action('rightArrowClick')}
        formId="formName1"
        buttonShow
      />
    </form> 
  </Story>
</Preview>

# Enabled Select Using a Default Value
<Preview>
  <Story name="Enabled Select Using a Default Value">
    <form>
      <Select 
        selectedValue={2}
        buttonClick={action('rightArrowClick')}
        name="enabledSelectUsingDefaultValue"
        label="Label is always required for accessbility"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
        handleChange={action('changed')}
        formId="formName2"
        buttonShow
      />
    </form> 
  </Story>
</Preview>

# Disabled Select Using Label  
<Preview>
  <Story name="Disabled Select ">
    <form>
      <Select 
        label = "Select from the below choices"
        handleChange={action('changed')}
        buttonClick={action('rightArrowClick')}
        formId="formName3"
        name="disabledSelect"
        buttonShow
        disabled
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form> 
  </Story>
</Preview>

# Select without right arrow 
<Preview>
  <Story name="Select without the right arrow">
    <form>
      <Select 
        label = "Select from the below choices"
        handleChange={action('changed')}
        formId="formName4"
        name="selectSimple"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form> 
  </Story>
</Preview>

# Select Error
<Preview>
  <Story name="Select Error">
    <form>
      <Select 
        label="Select from the below choices"
        handleChange={action('changed')}
        formId="formName5"
        name="Select Error no arrow"
        errorMsg="Error Message"
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form> 
  </Story>
</Preview>

# Select Error With Show Arrow
<Preview>
  <Story name="Select Error with right arrow">
    <form>
      <Select 
        label = "Select from the below choices"
        handleChange={action('changed')}
        formId="formName6"
        name="selectErrorWithButton"
        errorMsg="Error Message"
        buttonShow
        options={[
          {
           name: 'option 1',
           value: 1,
         },
         {
           name: 'option 2',
           value: 2,
          },
       ]}
      />
    </form> 
  </Story>
</Preview>







