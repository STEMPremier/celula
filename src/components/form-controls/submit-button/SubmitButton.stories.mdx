import { Description, Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, select, text } from '@storybook/addon-knobs';

import SubmitButton from '.';
import {
  BUTTON_TYPES as TYPES,
  COMPONENT_STATES as STATES,
  COLORS,
  SIZES
} from '../../../utils/constants';

<Meta component={SubmitButton} title="Components/Molecules/Form Controls/SubmitButton" />

# SubmitButton
<Description of={SubmitButton} />
<Props of={SubmitButton} />

<Story name="SubmitButton with Knobs" parameters={{ docs: { disable: true }}}>
  <SubmitButton
    className={text('className', '')}
    color={select('color', COLORS, 'primary')}
    disabled={boolean('disabled', false)}
    handleClick={action('`SubmitButton with Knobs` onClick event triggered.')}
    size={select('size', SIZES, 'small')}
    type={select('type', TYPES, 'default')}
  >
    {text('children', 'a button')}
  </SubmitButton>
</Story>

### Types
<Preview>
  <Story name="SubmitButton Types">
    <div>
      {TYPES.map(type => (
        <SubmitButton
          handleClick={action('`SubmitButton Types` onClick event triggered.')}
          key={type}
          type={type}
        >
          {`${type} button`}
        </SubmitButton>
      ))}
    </div>
  </Story>
</Preview>

### Sizes
<Preview>
  <Story name="SubmitButton Sizes">
    <div>
      {TYPES.map(type => (
        <div key={`${type}_size`}>
          {SIZES.map(size => (
            <SubmitButton
              handleClick={action('`SubmitButton Sizes` onClick event triggered.')}
              key={`${type}_${size}`}
              size={size}
              type={type}
            >
              {`${type} ${size}`}
            </SubmitButton>
          ))}
        </div>
      ))}
    </div>
  </Story>
</Preview>

### States
<Preview>
  <Story name="SubmitButton States">
    <div>
      {TYPES.map(type => (
        <div key={`${type}_state`}>
          {STATES.filter(state => state !== 'error').map(state => (
            <SubmitButton
              disabled={state === 'disabled'}
              handleClick={action('`SubmitButton States` onClick event triggered.')}
              key={`${state}_${type}`}
              type={type}
            >
              {`${state} ${type}`}
            </SubmitButton>
          ))}
        </div>
      ))}
    </div>
  </Story>
</Preview>

### Colors
<Preview>
  <Story name="SubmitButton Colors">
    <div>
      {TYPES.map(type => (
        <div key={`${type}_color`}>
          {COLORS.map(color => (
            <span
              style={color === 'inverted' ? {backgroundColor:'black',display:'inline-block'} : {}}
              key={`${type}_${color}`}
            >
              <SubmitButton
                color={color}
                handleClick={action('`SubmitButton Colors` onClick event triggered.')}
                type={type}
              >
                {color}
              </SubmitButton>
            </span>
          ))}
        </div>
      ))}
    </div>
  </Story>
</Preview>
