import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, select, text, array } from '@storybook/addon-knobs';

import  { Switch } from '.';

<Meta component={Switch} title="Components/Molecules/Form Controls/Switch" />

# Switch

<Description of={Switch} />
<Props of={Switch} />
<Story name="Switch with Knobs" parameters={{ docs: { disable: true } }}>
    <Switch
      checked={boolean('checked', false)}
      className={text('className', '')}
      disabled={boolean('disabled', false)}
      errorMsg={text('errorMsg', '')}
      handleClick={action('`Checkbox with Knobs` onClick event triggered.')}
      handleChange={action('`Switch with Knobs` onClick event triggered.')}
      label={text('label', 'Accept Terms and Conditions')}
      name={text('name', 'termsAndConditions')}
      value={text('value', 'accepted')}
    >
  </Switch>
</Story>

### Switch Unchecked
<Preview>
  <Story name="Switch Unchecked">
      <Switch
        label="Check this slider"
        name="checkSlider"
        value="sliderChecked"
      />
  </Story>
</Preview>

### Switch Checked
<Preview>
  <Story name="Switch Checked">
      <Switch
        checked
        label="Uncheck this slider"
        name="checkSlider"
        value="sliderChecked"
      />
  </Story>
</Preview>

### Switch Disabled Unchecked
<Preview>
  <Story name="Switch Disabled Unchecked">
      <Switch
        label="This slider is disabled and unchecked"
        name="checkSlider"
        value="sliderChecked"
        disabled
      />
  </Story>
</Preview>


### Switch Disabled Checked
<Preview>
  <Story name="Switches Disabled Checked">
      <Switch
        label="This slider is disabled and checked"
        name="checkSlider"
        value="sliderChecked"
        checked
        disabled
      />
  </Story>
</Preview>

### Switch Error
<Preview>
  <Story name="Switch Error">
      <Switch
        label="This slider is in an error state"
        name="checkSlider"
        value="sliderChecked"
        errorMsg="This is the wrong choice"
      />
  </Story>
</Preview>




