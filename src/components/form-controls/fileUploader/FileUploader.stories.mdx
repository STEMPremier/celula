import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, text } from '@storybook/addon-knobs';

import {
  COLORS,
  SIZES,
  FILEUPLOADER_TYPES as TYPES,
} from '../../../utils/constants';

import FileUploader from '.';

<Meta component={FileUploader} title="Components/Molecules/Form Controls/FileUploader" />

# FileUplaoder
<Description of={FileUploader} />
<Props of={FileUploader} />

<Story name="FileUplaoder with Knobs" parameters={{ docs: { disable: true } }}>
  <form id="fileUploaderWithKnobForm">
    <FileUploader
      className={text('className', '', 'FileUploader Options')}
      disabled={boolean('disabled', false, 'FileUploader Options')}
      errorMsg={text('errorMsg', '', 'FileUploader Options')}
      formId={text('formId', 'fileUploaderWithKnobForm', 'FileUploader Options')}
      handleClick={action('`FileUploader with Knobs` onChange event triggered.')}
      helpText={text(
        'helpText',
        'This is useful or helpful information.',
        'FileUploader Options',
      )}
      multiple={boolean('multiple', false, 'FileUploader Options')}
      name={text('name', 'fileUploaderWithKnobs', 'FileUploader Options')}
      size={text('size', 'large', 'FileUploader Options')}
      styleType={text('styleType', '', 'FileUploader Options')}
      value={text('value', '', 'FileUploader Options')}
    >{text('children', 'CHILDREN LABEL')}</FileUploader>
  </form>
</Story>

### Colors
<Preview>
  <Story name="FileUploader Colors">
    <div>
      {TYPES.map(type => (
        <div key={`${type}_color`}>
          {COLORS.map(color => (
            <form id="FileUploader Colors">
              <FileUploader 
                label="This are the other colors"
                name="This is a name"
                color={color}
                styleType={type}
              >
               {`${color} ${type}`}
             </FileUploader>
           </form>
           ))}
        </div>
      ))}
    </div>
    </Story>
  </Preview>

### Sizes 
<Preview>
  <Story name="FileUploader Sizes">
    {SIZES.map(size => (
      <form id="FileUploader Sizes">
      <FileUploader 
        label="This are the other sizes"
        name="This is a name"
        size={size}
      >
      {`${size} File Uploader`}
      </FileUploader>
      </form>
    ))}
    </Story>
  </Preview>

### Types 
<Preview>
  <Story name="FileUploader Types">
     <form id="FileUploader Types">
    {TYPES.map(type => (
      <FileUploader 
        label="These are the other types"
        name="This is a name"
        styleType={type}
      >
      {`${type} File Uploader`}
      </FileUploader>
    ))} </form>
    </Story>
  </Preview>

### Accept, Capture, Multiple Functionality 

### Disabled 