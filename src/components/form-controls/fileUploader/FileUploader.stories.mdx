import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, text } from '@storybook/addon-knobs';

import {
  COLORS,
  SIZES,
  FILEUPLOADER_TYPES as TYPES,
} from '../../../utils/constants';

import FileUploader from '.';

<Meta component={FileUploader} title="Components/Molecules/Form Controls/FileUploader" />

# FileUplaoder
<Description of={FileUploader} />
<Props of={FileUploader} />

<Story name="FileUplaoder with Knobs" parameters={{ docs: { disable: true } }}>
  <form id="fileUploaderWithKnobForm">
    <FileUploader
      accept={boolean('accept', false, 'FileUploader Options')}
      capture={boolean('capture', false, 'FileUploader Options')}
      className={text('className', '', 'FileUploader Options')}
      color={text('color', 'primary', 'FileUploader Options')}
      disabled={boolean('disabled', false, 'FileUploader Options')}
      errorMsg={text('errorMsg', '', 'FileUploader Options')}
      formId={text('formId', 'fileUploaderWithKnobForm', 'FileUploader Options')}
      handleClick={action('`FileUploader with Knobs` onChange event triggered.')} 
      multiple={boolean('multiple', false, 'FileUploader Options')}
      name={text('name', 'fileUploaderWithKnobs', 'FileUploader Options')}
      size={text('size', 'large', 'FileUploader Options')}
      styleType={text('styleType', 'default', 'FileUploader Options')}
      value={text('value', '', 'FileUploader Options')}
    >{text('children', 'CHILDREN LABEL')}</FileUploader>
  </form>
</Story>

### Colors
<Preview>
  <Story name="FileUploader Colors and Types">
    <div>
      {TYPES.map(type => (
        <div key={`${type}_color`}>
          {COLORS.map(color => (
            <form id="FileUploader Colors and Types">
              <FileUploader 
                label="This are the other colors"
                name="This is a name"
                color={color}
                styleType={type}
              >
               {`${color} ${type}`}
             </FileUploader>
           </form>
           ))}
        </div>
      ))}
    </div>
    </Story>
  </Preview>

### Sizes 
<Preview>
  <Story name="FileUploader Sizes">
    {SIZES.map(size => (
        <div key={`${size}`}>
          <form id="FileUploader Sizes">
            <FileUploader 
              label="These are the sizes"
             name="This is a name"
              size={size}
           >
            {`${size} File Uploader`}
           </FileUploader>
         </form>
           </div>
       ))}
    </Story>
  </Preview>

### Disabled 
<Preview>
  <Story name="FileUploader Disabled">
    <div>
      {TYPES.map(type => (
            <form id="FileUploader Disabled">
              <FileUploader 
                label="These are the disabled colors and types"
                name="This is a name"
                styleType={type}
                disabled
              >
               {` ${type}`}
             </FileUploader>
           </form>
      ))}
    </div>
    </Story>
  </Preview>

### Error State

<Preview>
  <Story name="FileUploader Error State">
    <div>
      {TYPES.map(type => (
            <form id="FileUploader Error State">
              <FileUploader 
                label="These are the disabled colors and types"
                name="This is a name"
                errorMsg={'You did not choose wisely!'}
                styleType={type}
              >
               {` ${type}`}
             </FileUploader>
           </form>
      ))}
    </div>
    </Story>
  </Preview>
