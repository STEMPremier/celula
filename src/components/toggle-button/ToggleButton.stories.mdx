import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, select, text } from '@storybook/addon-knobs';

import { ToggleButton, ToggleButtonGroup } from '.';
import { COMPONENT_STATES as STATES, COLORS, SIZES, SYSTEM_ICONS } from '../../utils/constants';

<Meta component={ToggleButton} title="Components/Atoms/ToggleButton" />

# ToggleButton
A `ToggleButton` is comprised of 2 seperate components; the individual buttons, `ToggleButton`, and their container, `ToggleButtonGroup`. `ToggleButtons` must always be inside a `TogglebuttonContainer` to function properly, and as a group.

## ToggleButton
<Description of={ToggleButton} />
<ArgsTable of={ToggleButton}/>

## ToggleButtonGroup
<Description of={ToggleButtonGroup} />
<ArgsTable of={ToggleButtonGroup}/>

<Story name="ToggleButtonGroup with Knobs" parameters={{docs:{disable:true}}}>
  <ToggleButtonGroup
    className={text('className', '')}
    color={select('color', COLORS, 'primary')}
    disabled={boolean('disabled', false)}
    formId={text('formId', 'formKnobs')}
    handleChange={action('`ToggleButtonGroup with Knobs` onChange event triggered.')}
    name={text('name', 'formKnobs')}
    selectedValue={select('selectedValue', ['export','follow', 'message'], 'follow')}
    size={select('size', SIZES, 'large')}
  >
    <ToggleButton icon="export" value="export" />
    <ToggleButton icon="follow" value="follow" />
    <ToggleButton icon="message" value="message" />
  </ToggleButtonGroup>
</Story>

<Story name="ToggleButton with Knobs" parameters={{docs:{disable:true}}}>
  <ToggleButtonGroup
    handleChange={action('`ToggleButton with Knobs` onChange event triggered.')}
    name="formKnobs2"
    selectedValue={text('selectedValue', 'follow')}
  >
    <ToggleButton
      className={text('className', '')}
      icon={select('icon', SYSTEM_ICONS,'export')}
      value={text('value','export')}
    />
    <ToggleButton icon="follow" value="follow" />
    <ToggleButton icon="message" value="message" />
  </ToggleButtonGroup>
</Story>

## Sizes
<Canvas>
  <Story name="Small">
    <ToggleButtonGroup
      handleChange={value => console.log(value)}
      name={`formSizes_small`}
      selectedValue="follow"
      size="small"
    >
      <ToggleButton icon="export" value="export" />
      <ToggleButton icon="follow" value="follow" />
      <ToggleButton icon="message" value="message" />
    </ToggleButtonGroup>
  </Story>
  <Story name="Large (Default)">
    <ToggleButtonGroup
      handleChange={value => console.log(value)}
      name={`formSizes_large`}
      selectedValue="follow"
      size="large"
    >
      <ToggleButton icon="export" value="export" />
      <ToggleButton icon="follow" value="follow" />
      <ToggleButton icon="message" value="message" />
    </ToggleButtonGroup>
  </Story>
  <Story name="Jumbo">
    <ToggleButtonGroup
      handleChange={value => console.log(value)}
      name={`formSizes_jumbo`}
      selectedValue="follow"
      size="jumbo"
    >
      <ToggleButton icon="export" value="export" />
      <ToggleButton icon="follow" value="follow" />
      <ToggleButton icon="message" value="message" />
    </ToggleButtonGroup>
  </Story>
</Canvas>

## Colors and States
<Canvas>
  <Story name="Primary (Default)">
    <ToggleButtonGroup
      color="primary"
      handleChange={value => console.log(value)}
      name={`formStates_primary`}
      selectedValue="follow"
    >
      <ToggleButton icon="export" value="export" />
      <ToggleButton icon="follow" value="follow" />
      <ToggleButton icon="message" value="message" />
    </ToggleButtonGroup>
  </Story>
  <Story name="Secondary">
    <ToggleButtonGroup
      color="secondary"
      handleChange={value => console.log(value)}
      name={`formStates_secondary`}
      selectedValue="follow"
    >
      <ToggleButton icon="export" value="export" />
      <ToggleButton icon="follow" value="follow" />
      <ToggleButton icon="message" value="message" />
    </ToggleButtonGroup>
  </Story>
  <Story name="Black">
    <ToggleButtonGroup
      color="black"
      handleChange={value => console.log(value)}
      name={`formStates_black`}
      selectedValue="follow"
    >
      <ToggleButton icon="export" value="export" />
      <ToggleButton icon="follow" value="follow" />
      <ToggleButton icon="message" value="message" />
    </ToggleButtonGroup>
  </Story>
  <Story name="Inverted">
    <div style={{backgroundColor:'black'}}>
      <ToggleButtonGroup
        color="inverted"
        handleChange={value => console.log(value)}
        name={`formStates_inverted`}
        selectedValue="follow"
      >
        <ToggleButton icon="export" value="export" />
        <ToggleButton icon="follow" value="follow" />
        <ToggleButton icon="message" value="message" />
      </ToggleButtonGroup>
    </div>
  </Story>
  <Story name="disabled">
    <ToggleButtonGroup
      disabled={true}
      handleChange={value => console.log(value)}
      name={`formStates_disabled`}
      selectedValue="follow"
    >
      <ToggleButton icon="export" value="export" />
      <ToggleButton icon="follow" value="follow" />
      <ToggleButton icon="message" value="message" />
    </ToggleButtonGroup>
  </Story>
</Canvas>
