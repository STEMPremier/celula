import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, text } from '@storybook/addon-knobs';

import Search from '.';

<Meta component={Search} title="Components/Atoms/Search" />

# Search
<Description of={Search} />
<ArgsTable of={Search} />

<Story name="Search with Knobs" parameters={{ docs: { disable: true } }}>
  <Search
    className={text('classNames', '')}
    disabled={boolean('disabled', false)}
    errorMsg={text('errorMsg', '')}
    instantSearch={boolean('instantSearch', false)}
    handleSearch={action('Search With Knobs `handleSearch` function triggered.')}
    helpText={text('helpText', 'Helpful text.')}
    label={text('label', 'Do a search:')}
    name={text('name', 'searchWithKnobs')}
    placeholder={text('placeholder', 'Enter search terms here')}
  />
</Story>

## Types
By default `<Search />` searches on click or press of the included button:
<Canvas>
  <Story name="On Click">
    <Search
      handleSearch={value => console.log(value)}
      helpText="Open the console, click the button."
      label="Search on Click:"
      name="searchTypesOnClick"
      placeholder="Enter search terms here"
    />
  </Story>
</Canvas>

It has the option of searcing as you type as well:
<Canvas>
  <Story name="As You Type">
    <Search
      handleSearch={value => console.log(value)}
      helpText="Clearing the search doesn't trigger the handleSearch function."
      instantSearch={true}
      label="Search as You Type:"
      name="searchTypesOnType"
      placeholder="Enter search terms here"
    />
  </Story>
</Canvas>

## States
A `<Search />` can be disabled:
<Canvas>
  <Story name="Disabled">
    <Search
      disabled={true}
      handleSearch={value => console.log(value)}
      helpText="Open the console, click the button."
      label="Disabled Search:"
      name="searchStatesDisabled"
      placeholder="Enter search terms here"
    />
  </Story>
</Canvas>

Or have an error:
<Canvas>
  <Story name="Error">
    <Search
      errorMsg="Whoops! Somthing is not okay."
      handleSearch={value => console.log(value)}
      helpText="Clearing the search doesn't trigger the handleSearch function."
      instantSearch={true}
      label="Search with Error:"
      name="searchStatesWithError"
      placeholder="Enter search terms here"
    />
  </Story>
</Canvas>
