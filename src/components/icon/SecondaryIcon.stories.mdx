import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';

import { SecondaryIcon } from '.';
import { SECONDARY_ICONS, SIZES } from '../../utils/constants';

<Meta
  args={{ name: 'pdf' }}
  component={SecondaryIcon}
  title="Components/Molecules/Icons/SecondaryIcon"
/>

# SecondaryIcon
<Description of={SecondaryIcon} />

<Canvas>
  <Story name="SecondaryIcon with Controls">
    {args => <SecondaryIcon {...args} />}
  </Story>
</Canvas>

<ArgsTable story="SecondaryIcon with Controls" />

## Icons

<Canvas>
  <Story name="SecondaryIcons">
    <div
      style={{
        display: 'flex',
        flexWrap: 'wrap',
        justifyContent: 'space-around',
      }}
    >
      {SECONDARY_ICONS.map((icon, idx) => {
        return (
          <div
            key={`${icon}_${idx}`}
            style={{
              alignItems: 'center',
              border: 'solid black 2px',
              borderRadius: '5px',
              display: 'flex',
              flexDirection: 'column',
              height: '40px',
              margin: '5px',
              padding: '15px',
              width:'40px',
            }}
          >
            <SecondaryIcon name={icon} />
            <span>{icon}</span>
          </div>
        );
      })}
    </div>
  </Story>
</Canvas>

## Colors
A `SecondaryIcon` can come in one of two colors.

<Canvas>
  <Story name="Primary SecondaryIcon">
    <div
      style={{
        alignItems: 'center',
        border: 'solid black 2px',
        borderRadius: '5px',
        display:'flex',
        flexDirection: 'column',
        margin: '5px',
        padding: '15px',
      }}
    >
      <SecondaryIcon name="pdf" />
      <span>pdf</span>
    </div>
  </Story>
  <Story name="Secondary SecondaryIcon">
    <div
      style={{
        alignItems: 'center',
        border: 'solid black 2px',
        borderRadius:'5px',
        display: 'flex',
        flexDirection: 'column',
        margin: '5px',
        padding: '15px',
      }}
    >
      <SecondaryIcon name="pdf" color="secondary" />
      <span>pdf</span>
    </div>
  </Story>
</Canvas>

## Options
You can even use a small amount of text (up to two characters) in place of an icon.

<Canvas>
  <Story name="SecondaryIcon with Text 1">
    <div
      style={{
        alignItems: 'center',
        border: 'solid black 2px',
        borderRadius: '5px',
        display: 'flex',
        flexDirection: 'column',
        margin: '5px',
        padding: '15px',
      }}
    >
      <SecondaryIcon text="A" />
      <span>w/text</span>
    </div>
  </Story>
  <Story name="SecondaryIcon with Text 2">
    <div
      style={{
        alignItems: 'center',
        border: 'solid black 2px',
        borderRadius: '5px',
        display: 'flex',
        flexDirection: 'column',
        margin: '5px',
        padding: '15px',
      }}
    >
      <SecondaryIcon text="+1" />
      <span>w/text</span>
    </div>
  </Story>
</Canvas>

## Sizes

<Canvas>
  <Story name="Small SecondaryIcon">
    <div
      style={{
        alignItems: 'center',
        border: 'solid black 2px',
        borderRadius: '5px',
        display: 'flex',
        flexDirection: 'column',
        margin: '5px',
        padding: '15px',
      }}
    >
      <SecondaryIcon name="pdf" />
      <span>small</span>
    </div>
  </Story>
  <Story name="Large SecondaryIcon">
    <div
      style={{
        alignItems: 'center',
        border: 'solid black 2px',
        borderRadius: '5px',
        display: 'flex',
        flexDirection: 'column',
        margin: '5px',
        padding: '15px',
      }}
    >
      <SecondaryIcon name="pdf" size="large" />
      <span>large</span>
    </div>
  </Story>
  <Story name="Jumbo SecondaryIcon">
    <div
      style={{
        alignItems: 'center',
        border: 'solid black 2px',
        borderRadius: '5px',
        display: 'flex',
        flexDirection: 'column',
        margin: '5px',
        padding: '15px',
      }}
    >
      <SecondaryIcon name="pdf" size="jumbo" />
      <span>jumbo</span>
    </div>
  </Story>
</Canvas>
