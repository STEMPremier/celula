import { Description, Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';

import { SystemIcon } from '.';
import { SYSTEM_ICONS } from './types';

<Meta component={SystemIcon} title="Components/Molecules/Icons/SystemIcon" />

# Icon
<Description of={SystemIcon} />
<Props of={SystemIcon} />

### System Icons
<Preview>
  <Story name="SystemIcons">
    <div style={{display:'flex',flexWrap:'wrap',justifyContent:'space-around'}}>
      {SYSTEM_ICONS.map((icon, idx) => {
        return (
          <div key={`${icon}_${idx}`} style={{margin:'5px',display:'flex',flexDirection:'column',alignItems:'center',padding:'15px',border:'solid black 1px',borderRadius:'5px',height:'25px',minWidth:'25px'}}>
            <SystemIcon name={icon} />
            <span>{icon}</span>
          </div>
        );
      })}
    </div>
  </Story>
</Preview>

### The Colors
<Preview>
  <Story name="SystemIcon Colors">
    <div style={{display:'flex',flexWrap:'wrap',justifyContent:'space-around'}}>
      {['black', 'blue', 'green', 'orange', 'purple', 'red'].map((color, idx) => {
        return (
          <div key={`${color}_${idx}`} style={{margin:'5px',display:'flex',flexDirection:'column',alignItems:'center',padding:'15px',border:'solid black 1px',borderRadius:'5px',height:'25px',minWidth:'25px'}}>
            <SystemIcon name="edit" color={color} />
            <span>{color}</span>
          </div>
        );
      })}
    </div>
  </Story>
</Preview>
