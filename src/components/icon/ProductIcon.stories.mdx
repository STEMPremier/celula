import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs/blocks';
import { select } from '@storybook/addon-knobs';

import { ProductIcon } from '.';
import { PRODUCT_ICONS, SIZES } from '../../utils/constants';

<Meta component={ProductIcon} title="Components/Molecules/Icons/ProductIcon" />

# ProductIcon
<Description of={ProductIcon} />
<ArgsTable of={ProductIcon} />

<Story name="ProductIcons with Knobs" parameters={{ docs: { disable: true }}}>
  <ProductIcon
    name={select('name', PRODUCT_ICONS, 'college')}
    size={select('size', SIZES, 'small')}
  />
</Story>

## Icons
<Story name="ProductIcons">
  <div style={{display:'flex',flexWrap:'wrap',justifyContent:'space-around'}}>
    {PRODUCT_ICONS.map((icon, idx) => {
      return (
        <div key={`${icon}_${idx}`} style={{margin:'5px',display:'flex',flexDirection:'column',alignItems:'center',padding:'15px',border:'solid black 2px',borderRadius:'5px',height:'75px',width:'75px'}}>
          <ProductIcon name={icon} />
          <span>{icon}</span>
        </div>
      );
    })}
  </div>
</Story>

## Sizes
<Canvas>
  <Story name="Small (Default)">
    <div style={{margin:'5px',display:'flex',flexDirection:'column',alignItems:'center',padding:'15px',border:'solid black 2px',borderRadius:'5px',height:'75px',width:'75px'}}>
      <ProductIcon name="college" />
      <span>small</span>
    </div>
  </Story>
  <Story name="Large">
    <div style={{margin:'5px',display:'flex',flexDirection:'column',alignItems:'center',padding:'15px',border:'solid black 2px',borderRadius:'5px',height:'100px',width:'100px'}}>
      <ProductIcon name="college" size="large" />
      <span>large</span>
    </div>
  </Story>
  <Story name="Jumbo">
    <div style={{margin:'5px',display:'flex',flexDirection:'column',alignItems:'center',padding:'15px',border:'solid black 2px',borderRadius:'5px',height:'125px',width:'125px'}}>
      <ProductIcon name="college" size="jumbo" />
      <span>jumbo</span>
    </div>
  </Story>
</Canvas>
