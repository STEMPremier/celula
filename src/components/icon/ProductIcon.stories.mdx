import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';

import { ProductIcon } from '.';

import { PRODUCT_ICONS, SIZES } from '../../utils/constants';

<Meta
  args={{ name: 'college' }}
  component={ProductIcon}
  title="Components/Molecules/Icons/ProductIcon"
/>

# ProductIcon
<Description of={ProductIcon} />

<Canvas>
  <Story name="ProductIcons with Controls">
    {args => <ProductIcon {...args} />}
  </Story>
</Canvas>

<ArgsTable story="ProductIcons with Controls" />

## Icons

<Story name="ProductIcons">
  <div
    style={{
      display: 'flex',
      flexWrap: 'wrap',
      justifyContent: 'space-around',
    }}
  >
    {PRODUCT_ICONS.map((icon, idx) => {
      return (
        <div
          key={`${icon}_${idx}`}
          style={{
            alignItems: 'center',
            border: 'solid black 2px',
            borderRadius: '5px',
            display: 'flex',
            flexDirection: 'column',
            height: '75px',
            margin: '5px',
            padding: '15px',
            width: '75px',
          }}
        >
          <ProductIcon name={icon} />
          <span>{icon}</span>
        </div>
      );
    })}
  </div>
</Story>

## Sizes

<Canvas>
  <Story name="Small ProductIcon">
    <div
      style={{
        alignItems: 'center',
        border: 'solid black 2px',
        borderRadius: '5px',
        display: 'flex',
        flexDirection: 'column',
        height: '75px',
        margin: '5px',
        padding: '15px',
        width: '75px',
      }}
    >
      <ProductIcon name="college" />
      <span>small</span>
    </div>
  </Story>
  <Story name="Large ProductIcon">
    <div
      style={{
        alignItems: 'center',
        border: 'solid black 2px',
        borderRadius: '5px',
        display: 'flex',
        flexDirection: 'column',
        height: '100px',
        margin: '5px',
        padding: '15px',
        width: '100px',
      }}>
      <ProductIcon name="college" size="large" />
      <span>large</span>
    </div>
  </Story>
  <Story name="Jumbo ProductIcon">
    <div
      style={{
        alignItems: 'center',
        border: 'solid black 2px',
        borderRadius: '5px',
        display: 'flex',
        flexDirection: 'column',
        height: '125px',
        margin: '5px',
        padding: '15px',
        width: '125px',
      }}
    >
      <ProductIcon name="college" size="jumbo" />
      <span>jumbo</span>
    </div>
  </Story>
</Canvas>
