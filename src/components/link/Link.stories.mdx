import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { select, text } from '@storybook/addon-knobs';

import { COLORS, LINK_TEXT_STYLES as TYPES } from '../../utils/constants';

import Link from '.';

<Meta component={Link} title="Components/Atoms/Link"/>

# Links
<Description of={Link}/>
<ArgsTable of={Link}/>

<Story name="Link with Knobs" parameters={{ docs: { disable: true } }}>
  <Link
    address={text('address','https://app.tallo.com/register' )}
    className={text('className', '')}
    color={select('color', COLORS, 'primary')}
    text={text('text', 'Tallo')}
    textStyle={select('textStyle', TYPES, 'uppercase' )}
  />
</Story>

## Uppercase
<Canvas>
  <Story name="Uppercase (Primary)">
    <Link
      address="#"
      text="link"
      textStyle="uppercase"
    />
  </Story>
</Canvas>

### Colors
<Canvas>
  <Story name="Uppercase Primary (Default)">
    <Link
      address="#"
      color="primary"
      text="primary"
    />
  </Story>
  <Story name="Uppercase Secondary">
    <Link
      address="#"
      color="secondary"
      text="secondary"
    />
  </Story>
  <Story name="Uppercase Black">
    <Link
      address="#"
      color="black"
      text="black"
    />
  </Story>
  <Story name="Uppercase Inverted">
    <div style={{backgroundColor:'black'}}>
      <Link
        address="#"
        color="inverted"
        text="inverted"
      />
    </div>
  </Story>
</Canvas>

## Lowercase
<Canvas>
  <Story name="Lowercase">
    <Link
      address="#"
      text="link"
      textStyle="lowercase"
    />
  </Story>
</Canvas>

### Colors
<Canvas>
  <Story name="Lowercase Primary (Default)">
    <Link
      address="#"
      color="primary"
      text="primary"
      textStyle="lowercase"
    />
  </Story>
  <Story name="Lowercase Secondary">
    <Link
      address="#"
      color="secondary"
      text="secondary"
      textStyle="lowercase"
    />
  </Story>
  <Story name="Lowercase Black">
    <Link
      address="#"
      color="black"
      text="black"
      textStyle="lowercase"
    />
  </Story>
  <Story name="Lowercase Inverted">
    <div style={{backgroundColor:'black'}}>
      <Link
        address="#"
        color="inverted"
        text="inverted"
        textStyle="lowercase"
      />
    </div>
  </Story>
</Canvas>
