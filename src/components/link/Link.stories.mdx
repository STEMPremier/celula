import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, select, text } from '@storybook/addon-knobs';

import Link from '.';
import { LINK_TEXT_STYLES as TYPES, LINK_COLORS as COLORS } from '../../utils/constants';

<Meta component={Link} title="Components/Atoms/Link"/>

# Links 
<Description of={Link}/>
<Props of={Link}/>

<Story name="Link with Knobs" parameters={{ docs: { disable: true}}}>
  <Link 
  className={text('className', '', 'Link Options')}
  textStyle={text('textStyle', 'uppercase' )}
  address={text('address','https://app.tallo.com/register' )}
  color={text('color', 'blue')}
  />
</Story>

### Types 
<Preview>
  <Story name="Link Types">
    {TYPES.map(type => (
        <Link 
          key={type}
          text={type}
          textStyle={type}
        />
    ))}
  </Story>
</Preview>

### Colors
<Preview>
  <Story name="Link Colors">
    {COLORS.map(color => (
        <Link 
          key={color}
          text={color}
          color={color}
        />
    ))}
  </Story>
</Preview>