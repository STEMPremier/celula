import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';

import Pagination from '.';
import SomethingToPage from './SomethingToPage';

<Meta
  args={{
    currentPage: 4,
    gotoPage: () => {},
    nextPage: () => {},
    pageCount: 7,
    prevPage: () => {},
  }}
  component={Pagination}
  title="Components/Atoms/Pagination"
/>

# Pagination
<Description of={Pagination} />

<Canvas>
  <Story name="Pagination with Controls">
    {args => (
      <SomethingToPage
        canNext={args.canNextPage}
        canPrev={args.canPreviousPage}
        initialPage={args.currentPage}
        pageCount={args.pageCount}
      >
        <Pagination {...args} />
      </SomethingToPage>
    )}
  </Story>
</Canvas>

<ArgsTable story="Pagination with Controls" />

<Canvas>
  <Story name="Pagination">
    <Pagination
      canNextPage={true}
      canPreviousPage={true}
      currentPage={3}
      gotoPage={() => console.log('Navigate to a different page.')}
      nextPage={() => console.log('Navigate up a page.')}
      pageCount={10}
      prevPage={() => console.log('Navigate down a page.')}
    />
  </Story>
</Canvas>
