import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';

import Container from '.';

<Meta
  args={{
    border: true,
    children: 'This is stuff in a Container.',
    gap: 'padding',
  }}
  component={Container}
  title="Components/Atoms/Container"
/>

# Container
<Description of={Container} />

<Canvas>
  <Story name="Container with Controls">
    {args => <Container {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Container with Controls" />

## Spacing
A `Container` may include a gap. That gap can be margin or padding.

<Canvas isColumn>
  <Story name="Container with Margin">
    <Container
      border={true}
      gap="margin"
    >
      <span>This is stuff in a container with margin.</span>
    </Container>
  </Story>
  <Story name="Container with Padding">
    <Container
      border={true}
      gap="padding"
    >
      <span>This is stuff in a container with padding.</span>
    </Container>
  </Story>
  <Story name="Container with No Gap">
    <Container
      border={true}
      gap="none"
    >
      <span>This is stuff in a container with no gap.</span>
    </Container>
  </Story>
</Canvas>

## Border
A `Container` may include a border.

<Canvas isColumn>
  <Story name="Container with Border">
    <Container
      border={true}
      gap="padding"
    >
      <span>This is stuff in a container.</span>
    </Container>
  </Story>
  <Story name="Container with No Border">
    <Container
      border={false}
      gap="padding"
    >
      <span>This is stuff in a container.</span>
    </Container>
  </Story>
</Canvas>
