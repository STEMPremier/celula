import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs/blocks';
import { boolean, select } from '@storybook/addon-knobs';

import Container from '.';

<Meta component={Container} title="Components/Atoms/Container" />

# Container
<Description of={Container} />
<ArgsTable of={Container} />

<Story name="Container with Knobs" parameters={{ docs: { disable: true } }}>
  <Container
    border={boolean('border', false)}
    gap={select('gap', ['margin', 'padding', 'none'], 'margin')}
  >
    <span>This is stuff in a container.</span>
  </Container>
</Story>

## Spacing
### Margin
<Canvas>
  <Story name="Margin Gap (Default)">
    <Container
      border={true}
      gap="margin"
    >
      <span>This is stuff in a container with margin.</span>
    </Container>
  </Story>
</Canvas>

### Padding
<Canvas>
  <Story name="Padding Gap">
    <Container
      border={true}
      gap="padding"
    >
      <span>This is stuff in a container with padding.</span>
    </Container>
  </Story>
</Canvas>

### None
<Canvas>
  <Story name="No Gap">
    <Container
      border={true}
      gap="none"
    >
      <span>This is stuff in a container with no gap.</span>
    </Container>
  </Story>
</Canvas>

## Border
### With
<Canvas>
  <Story name="Border">
    <Container
      border={true}
      gap="padding"
    >
      <span>This is stuff in a container.</span>
    </Container>
  </Story>
</Canvas>

### Without
<Canvas>
  <Story name="No Border (Default)">
    <Container
      border={false}
      gap="padding"
    >
      <span>This is stuff in a container.</span>
    </Container>
  </Story>
</Canvas>
