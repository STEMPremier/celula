This is information about our button component.

```jsx
import Button from './Button';

<Button
  className="ce-button--text ce-button--blue "
  htmlType="button"
  onClick="handleClick"
>
  text button
</Button>;
```

```jsx
import Button from './Button';

<Button
  className="ce-button--text small ce-button--blue "
  htmlType="button"
  onClick="handleClick"
>
  text button
</Button>;
```

```jsx
import Button from './Button';

<Button
  className="ce-button--text ce-button--red"
  htmlType="button"
  onClick="handleClick"
>
  text button
</Button>;
```

```jsx
import Button from './Button';

<Button
  className="ce-button--text ce-button--red small"
  htmlType="button"
  onClick="handleClick"
>
  text button
</Button>;
```

```jsx
import Button from './Button';

<Button
  className="ce-button--outline ce-button--jumbo"
  htmlType="button"
  onClick="handleClick"
>
  <span>outline jumbo</span>
</Button>;
```

```jsx
import Button from './Button';

<Button
  className="ce-button--outline ce-button--large"
  htmlType="button"
  onClick="handleClick"
>
  <span>outline large</span>
</Button>;
```

```jsx
import Button from './Button';

<Button
  className="ce-button--outline ce-button--small"
  htmlType="button"
  onClick="handleClick"
>
  <span>outline small</span>
</Button>;
```

```jsx
import Button from './Button';

<Button
  className="ce-button--solid ce-button--jumbo"
  htmlType="button"
  onClick="handleClick"
>
  <span>solid jumbo</span>
</Button>;
```

```jsx
import Button from './Button';

<Button
  className="ce-button--solid ce-button--large"
  htmlType="button"
  onClick="handleClick"
>
  <span>solid large</span>
</Button>;
```

```jsx
import Button from './Button';

<Button
  className="ce-button--solid ce-button--small"
  htmlType="button"
  onClick="handleClick"
>
  <span>solid small</span>
</Button>;
```

> This text comes from a markdown file in the `/src/components/button` dir.
