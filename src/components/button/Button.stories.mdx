import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import Button from '.';

<Meta
  args={{
    children: 'Click Me!',
    handleClick: action('Clicked!'),
  }}
  component={Button}
  title="Components/Atoms/Button"
/>

# Button
<Description of={Button} />

<Canvas>
  <Story name="Button with Controls">
    {args => <Button {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Button with Controls" />

## Types
Solid buttons are high-emphasis, distinguished by their use of elevation and fill. They contain
actions that are primary to an application.

Outlined buttons are medium-emphasis buttons. They contain actions that are important, but aren’t
the primary action in an app. Outlined buttons are also a lower emphasis alternative to solid
buttons, or a higher emphasis alternative to text buttons.

Text buttons are typically used for less-pronounced actions, including those located in dialogs and
cards. They are often embedded in contained components like cards and dialogs, in order to relate
themselves to the component in which they appear. Because text buttons don’t have a container,
they don’t distract from nearby content. For consistency when using them with other buttons, text
buttons maintain the same margin and padding between the label and the edge of the container as the
other types.

<Canvas>
  <Story name="Solid Button">
    <Button
      handleClick={action('Solid button clicked.')}
    >
      solid button
    </Button>
  </Story>
  <Story name="Outline Button">
    <Button
      handleClick={action('Outline button clicked.')}
      type="outline"
    >
      outilne button
    </Button>
  </Story>
  <Story name="Text Button">
    <Button
      handleClick={action('Text button clicked.')}
      type="text"
    >
      text button
    </Button>
  </Story>
</Canvas>

## Sizes

<Canvas>
  <Story name="Small Solid Button">
    <Button
      handleClick={action('Small button clicked')}
      size="small"
    >
      small button
    </Button>
  </Story>
  <Story name="Large Solid Button">
    <Button
      handleClick={action('Large button clicked.')}
    >
      large button
    </Button>
  </Story>
  <Story name="Jumbo Solid Button">
    <Button
      handleClick={action('Jumbo button clicked.')}
      size="jumbo"
    >
      jumbo button
    </Button>
  </Story>
</Canvas>

<Canvas>
  <Story name="Small Outline Button">
    <Button
      handleClick={action('Small outline button clicked.')}
      size="small"
      type="outline"
    >
      small button
    </Button>
  </Story>
  <Story name="Large Outline Button">
    <Button
      handleClick={action('Large outline button clicked.')}
      type="outline"
    >
      large button
    </Button>
  </Story>
  <Story name="Jumbo Outline Button">
    <Button
      handleClick={action('Jumbo outline button clicked.')}
      size="jumbo"
      type="outline"
    >
      jumbo button
    </Button>
  </Story>
</Canvas>

<Canvas>
  <Story name="Small Text Button">
    <Button
      handleClick={action('Small text button clicked.')}
      type="text"
      size="small"
    >
      small button
    </Button>
  </Story>
  <Story name="Large Text Button">
    <Button
      handleClick={action('Large text button clicked.')}
      type="text"
    >
      large button
    </Button>
  </Story>
  <Story name="Jumbo Text Button">
    <Button
      handleClick={action('Jumbo text button clicked.')}
      size="jumbo"
      type="text"
    >
      jumbo button
    </Button>
  </Story>
</Canvas>

## Colors and States

<Canvas>
  <Story name="Solid Primary Button">
    <Button
      color="primary"
      handleClick={action('Solid primary button clicked.')}
      size="small"
    >
      primary
    </Button>
  </Story>
  <Story name="Solid Secondary Button">
    <Button
      color="secondary"
      handleClick={action('Solid secondary button clicked.')}
      size="small"
    >
      secondary
    </Button>
  </Story>
  <Story name="Solid Black Button">
    <Button
      color="black"
      handleClick={action('Solid black button clicked.')}
      size="small"
    >
      black
    </Button>
  </Story>
  <Story name="Solid Inverted Button">
    <div style={{ backgroundColor:'black', display:'inline-block' }}>
      <Button
        color="inverted"
        handleClick={action('Solid inverted button clicked.')}
        size="small"
      >
        inverted
      </Button>
    </div>
  </Story>
  <Story name="Solid Disabled Button">
    <Button
      disabled={true}
      handleClick={action('You will never trigger this action.')}
      size="small"
    >
      disabled
    </Button>
  </Story>
</Canvas>

<Canvas>
  <Story name="Outline Primary Button">
    <Button
      color="primary"
      handleClick={action('Outline primary button clicked.')}
      size="small"
      type="outline"
    >
      primary
    </Button>
  </Story>
  <Story name="Outline Secondary Button">
    <Button
      color="secondary"
      handleClick={action('Outline secondary button clicked.')}
      size="small"
      type="outline"
    >
      secondary
    </Button>
  </Story>
  <Story name="Outline Black Button">
    <Button
      color="black"
      handleClick={action('Outline black button clicked.')}
      size="small"
      type="outline"
    >
      black
    </Button>
  </Story>
  <Story name="Outline Inverted Button">
    <div style={{ backgroundColor:'black', display:'inline-block' }}>
      <Button
        color="inverted"
        handleClick={action('Outline inverted button clicked.')}
        size="small"
        type="outline"
      >
        inverted
      </Button>
    </div>
  </Story>
  <Story name="Outline Disabled Button">
    <Button
      disabled={true}
      handleClick={action('You will never trigger this action.')}
      size="small"
      type="outline"
    >
      disabled
    </Button>
  </Story>
</Canvas>

<Canvas>
  <Story name="Text Primary Button">
    <Button
      color="primary"
      handleClick={action('Primary text button clicked.')}
      size="small"
      type="text"
    >
      primary
    </Button>
  </Story>
  <Story name="Text Secondary Button">
    <Button
      color="secondary"
      handleClick={action('Secondary text button clicked.')}
      size="small"
      type="text"
    >
      secondary
    </Button>
  </Story>
  <Story name="Text Black Button">
    <Button
      color="black"
      handleClick={action('Black text button clicked.')}
      size="small"
      type="text"
    >
      black
    </Button>
  </Story>
  <Story name="Text Inverted Button">
    <div style={{ backgroundColor:'black', display:'inline-block' }}>
      <Button
        color="inverted"
        handleClick={action('Inverted text button clicked.')}
        size="small"
        type="text"
      >
        inverted
      </Button>
    </div>
  </Story>
  <Story name="Text Disabled Button">
    <Button
      disabled={true}
      handleClick={action('You will never trigger this action.')}
      size="small"
      type="text"
    >
      disabled
    </Button>
  </Story>
</Canvas>

## Multiple Buttons
If you place `Buttons` next to one another, there will be a 10px margin between them.

<Canvas>
  <Story name="Multiple Buttons">
    <Button
      handleClick={action('This button clicked.')}
    >
      this button
    </Button>
    <Button
      handleClick={action('That button clicked.')}
    >
      that button
    </Button>
  </Story>
</Canvas>
