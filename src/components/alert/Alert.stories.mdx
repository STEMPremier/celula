import {
  Description,
  Meta,
  Preview,
  Props,
  Story,
} from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { boolean, select, text } from '@storybook/addon-knobs';

import Alert from '.';
import { ALERT_TYPES as TYPES, SYSTEM_ICONS as ICONS } from '../../utils/constants';

<Meta component={Alert} title="Components/Molecules/Alerts" />

# Alerts

<Description of={Alert} />
<Props of={Alert} />

<Story name="Alert with Knobs" parameters={{docs: {disable: true}}}>
  <Alert
    className={text('className', '', 'Alert Options')}
    type={text('type', 'info', 'Alert options')}
    iconLeft={text('iconLeft', '', 'Secondary Icon Options')}
    dismissible={boolean('dismissible', false, 'Alert Options')}
    linkOptions={{
      name: text('name','Alert link text', 'Link Options'),
      address:text('address', 'http address', 'Link Options'),
    }}
    text={text('text', 'alert text', 'Alert Optionss')}
  ></Alert>
  </Story>

### Types 
<Preview>
  <Story name="Alert Types">
    {TYPES.map(type => (
      <div key={type}>
        <Alert 
          type={type}
          text={type}
       >
       </Alert>
      </div>
  ))}
  </Story>
  </Preview>

### Left Icons 
<Preview>
  <Story name="Alert Icons">
    {ICONS.map(icon => (
      <div key={icon}>
        <Alert 
          text={icon}
          iconLeft={icon}
       >
       </Alert>
      </div>
  ))}
  </Story>
  </Preview>

### Links 
<Preview>
   <Story name="Links by Types">
    {TYPES.map(type => (
      <div key={type}>
        <Alert 
          type={type}
          text={type}
          name='Link Name Here'
          address='https://app.tallo.com'
       >
       </Alert>
      </div>
    ))} 
    </Story>
  </Preview>

### Dismissible Alerts
<Preview>
   <Story name="Dismissible Alerts by Types">
    {TYPES.map(type => (
      <div key={type}>
        <Alert 
          type={type}
          text={type}
          dismissible
       >
       </Alert>
      </div>
    ))} 
    </Story>
  </Preview>


### Dismissible Alert with Icons and Links
<Preview>
    <Story name="Dismissible Alerts with Icons and Links">
    {ICONS.map(icon => (
      <div key={icon}>
        <Alert 
          text={icon}
          iconLeft={icon}
          dismissible
       >
       </Alert>
      </div>
    ))}
    </Story>
  </Preview>


### Dismissible Alert with Icons only
<Preview>
    <Story name="Dismissible Alerts with Icons only">
    {ICONS.map(icon => (
      <div key={icon}>
        <Alert 
          text={icon}
          iconLeft={icon}
          dismissible
       >
       </Alert>
      </div>
    ))}
    </Story>
  </Preview>


### Dismissible Alert with Links only
<Preview>
  <Story name="Dismissible Alerts with Links only">
    <Alert 
      text="Dismissible Alert with Link only"
      dismissible
    >
    </Alert>
  </Story>
</Preview>



